"use client"

const InputCadastro = ({ label, inputType, name, value, handleChange}) => {
    return (
        <div >
            {/* <label htmlFor={name}>
                {label}
            </label>
            <input
                type={inputType}
                id={name}
                name={name}
                value={value}
                onChange={handleChange}
                required
            /> */}
            teste
        </div>
    )
}

export default InputCadastro